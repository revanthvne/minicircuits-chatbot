// Vercel serverless entry point.
// Vercel routes all requests here (see vercel.json) and invokes the exported
// Express app as a Node serverless function.
module.exports = require('../server.js');
